{
    "name": "chat-manager-reload",
    "displayName": "VS Code chat manager - reload",
    "description": "Offers to reload the window after VS-code-chat-manager deletes a chat, or runs a queued prompt into one this window still has open, so the panel shows what changed.",
    "version": "2.0.0",
    "publisher": "phal40lax78",
    "license": "MIT",
    "engines": {
        "vscode": "^1.75.0"
    },
    "categories": [
        "Other"
    ],
    "activationEvents": [
        "onStartupFinished"
    ],
    "main": "./extension.js",
    "contributes": {
        "configuration": {
            "title": "VS Code chat manager - reload",
            "properties": {
                "chatManagerReload.signalFile": {
                    "type": "string",
                    "default": "",
                    "markdownDescription": "Path to the tool's `data/reload-request`. Empty means `~/Tools/VS-code-chat-manager/data/reload-request`. Set this if `VS-code-chat-manager.ps1` lives somewhere else - `data/` follows the script, so the path moves with it. `chatinstall` prints the line to paste when it is not the default."
                },
                "chatManagerReload.autoReload": {
                    "type": "boolean",
                    "default": false,
                    "markdownDescription": "Reload without asking after a delete. Off by default: a reload restarts every extension in the window, so one taken mid-answer loses that answer. Never applies while a chat in the workspace is still working (a turn, a permission prompt, a workflow or background agent), nor to a queued run, which can finish at any hour while another chat in the window is busy."
                }
            }
        }
    }
}
